name: qrtracing
description: Simple Contact Tracing Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  qr_code_scanner: ^0.0.13
  device_info: ^0.4.2+4
  provider: ^4.3.1
  firebase_core: ^0.4.5
  cloud_firestore: ^0.13.7
  firebase_messaging: ^6.0.16
  flutter_secure_storage: ^3.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0

flutter:
  uses-material-design: true
